---
description: One-line description of what this command triggers.
---

Run the `<name>` pipeline (see `pipelines/<name>.md`). This same workflow also triggers from a natural-language prompt matching "<trigger phrase>" or similar.

All workflow logic lives in the pipeline. This command is a thin trigger only.
