---
description: Run the feature pipeline (new → research → design → plan → implement).
---

Run the `feature` pipeline (see `pipelines/feature.md`). This same workflow also triggers from a natural-language prompt matching "new feature", "add feature", or similar.

All workflow logic lives in the pipeline. This command is a thin trigger only.
