node_modules
dist
*.tsbuildinfo
.DS_Store

# Preserved eval workspaces — kept locally for --rescore, never committed.
evals/arms/runs/

# Eval workspaces — kept locally for --rescore, never committed.
evals/refute/runs/
evals/route/runs/
